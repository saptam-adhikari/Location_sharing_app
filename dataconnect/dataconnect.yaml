specVersion: "v1beta"
serviceId: "location"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "location-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
